[project]
name = "lusmaker"
version = "0.1.0"
description = "Bouw stap voor stap fiets-GPX-lussen (Vlaamse Ardennen) via een CLI die door een LLM aangestuurd kan worden."
requires-python = ">=3.11"
dependencies = [
    "mcp>=2.0,<3",
    "osmium>=4.0",
    "numpy>=1.26",
    "PyJWT[crypto]>=2.10,<3",
    "PyYAML>=6.0",
]

[project.optional-dependencies]
aws = ["boto3>=1.40,<2", "uvicorn>=0.35,<1"]

[project.scripts]
lus = "lusmaker.cli:main"
lus-mcp = "lusmaker.mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["lusmaker"]
